build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
build.dir=build
build.generated.sources.dir=${build.dir}/generated-sources
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
compile.on.save=true
dist.dir=dist
dist.jar=${dist.dir}/CafeManagementSystem.jar
excludes=
includes=**
jar.compress=false
javac.classpath=lib/mysql-connector-j-9.7.0.jar
javac.modulepath=
javac.source=17
javac.target=17
javac.test.classpath=${javac.classpath}:${build.classes.dir}
main.class=com.cafe.gui.LoginPanel
manifest.file=manifest.mf
platform.active=default_platform
run.classpath=lib/mysql-connector-j-9.7.0.jar:${build.classes.dir}
run.modulepath=
run.test.classpath=${javac.test.classpath}:${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
